syntax = "proto3";

package jnoj.interface.v1;
import "errors/errors.proto";

option go_package = "api/interface/v1;v1";

enum SubmissionErrorReason {
    option (errors.default_code) = 500;
    SUBMISSION_RATE_LIMIT = 0 [(errors.code) = 429];
}
